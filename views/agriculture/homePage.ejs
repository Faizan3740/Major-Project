<% layout("./layouts/boilerplate") %>

<style>
  body {
    margin: 0;
    font-family: 'Poppins', Arial, sans-serif;
    background: linear-gradient(to bottom, #f4f4f4, #e8e8e8);
    color: #333;
  }

  .parent {
    display: flex;
    flex-wrap: wrap;
    align-items: stretch;
    justify-content: center;
    gap: 20px;
    padding: 20px;
    min-height: 100vh;
  }

  .card {
    margin-top: 150px;
    width: 320px;
    height: 420px; /* Ensures all cards are the same height */
    background: #fff;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: 10px;
    overflow: hidden;
    text-align: center;
    display: flex;
    flex-direction: column;
    transition: transform 0.3s, box-shadow 0.3s;
  }

  .card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
  }

  .card-img-top {
    width: 100%;
    height: 200px;
    object-fit: cover;
    border-bottom: 1px solid #ddd;
    filter: brightness(0.9);
    transition: filter 0.3s;
  }

  .card-img-top:hover {
    filter: brightness(1);
  }

  .card-body {
    flex-grow: 1; /* Ensures equal spacing in all cards */
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .card-title {
    font-size: 20px;
    margin-bottom: 15px;
    font-weight: bold;
    color: #222;
  }

  .card-text {
    font-size: 14px;
    margin-bottom: 20px;
    color: #555;
  }

  .btn {
    padding: 12px 25px;
    font-size: 14px;
    font-weight: bold;
    border-radius: 5px;
    transition: background 0.3s, color 0.3s;
  }

  .btn-success {
    background: linear-gradient(to right, #28a745, #218838);
    color: #fff;
    border: none;
  }

  .btn-success:hover {
    background: linear-gradient(to right, #218838, #1e7e34);
  }

  .btn-warning {
    background: linear-gradient(to right, #ffc107, #e0a800);
    color: #fff;
    border: none;
  }

  .btn-warning:hover {
    background: linear-gradient(to right, #e0a800, #c69500);
  }

  /* Preloader Styles */
  #preloader {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #080808;
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
  }

  #preloader .loader {
    border: 5px solid #f4f4f4;
    border-top: 5px solid #28a745;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }
</style>


<!-- Preloader -->
<div id="preloader">
  <div class="loader"></div>
</div>

<!-- Main Content -->
<div class="parent homePage">
  <!-- Farmer Card -->
  <div class="card">
    <img class="card-img-top" src="/images/Farmer.jpg" alt="Portal For Farmers">
    <div class="card-body">
      <h5 class="card-title">Portal For Farmers</h5>
      <p class="card-text">Login to access your account. Sign up if you are new to IntegratedCare.</p>
      <div class="d-flex justify-content-center gap-2">
        <form method="get" action="/loginUser">
          <button class="btn btn-success">Login</button>
        </form>
        <form method="get" action="/signUpUser">
          <button class="btn btn-warning">Sign Up</button>
        </form>
      </div>
    </div>
  </div>

  <!-- Government Authority Card -->
  <div class="card">
    <img class="card-img-top" src="/images/govtAuthority.jpg" alt="Portal For Authorized Candidate">
    <div class="card-body">
      <h5 class="card-title">Portal For Authorized Candidate</h5>
      <p class="card-text">Login to IntegratedCare to access your account.</p>
      <form method="get" action="/loginAuthority">
        <button class="btn btn-success">Login</button>
      </form>
    </div>
  </div>

  <!-- Agro-Chemist Card -->
  <div class="card">
    <img class="card-img-top" src="/images/agroChemist.jpg" alt="Portal For Agro-Chemists">
    <div class="card-body">
      <h5 class="card-title">Portal For Agro-Chemists</h5>
      <p class="card-text">Login to IntegratedCare to access your account.</p>
      <form method="get" action="/loginFarmer">
        <button class="btn btn-success">Login</button>
      </form>
    </div>
  </div>
</div>
